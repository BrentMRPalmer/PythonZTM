# Pip install 

import pyjokes

print(pyjokes.get_joke())
# Why don't jokes work in octal? Because 7 10 11.

# Can also use pip uninstall pyjokes
# or pip install pyjokes==0.4.0 to control the version installed

# pipenv can be used for virtual environments
# virtual environments are used to use different versions
# of the same package across different projects

# Pycharm has virtual environments by default (venv folder)